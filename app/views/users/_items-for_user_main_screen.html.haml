.Pictures-list
  - if @items.present?
    - @items.each do |item|
      .Pictures-list__items
        - if item.buyer.present?
          .Pictures-list__items__status
            .Pictures-list__items__status__inner
              Sold
        = link_to item_path(item.id), class:".Pictures-list__items__link" do
          %figure.Pictures-list__items__link__picture
            = image_tag item.images.first.url.url
          .Pictures-list__items__link__texts
            %h3.pict-name
              = item.name
            .pict-detail
              %ul
                %li 
                  = "#{item.price}円"
                %li
                  = icon('fa', 'star likeIcon')
              %p (税込)
